🌐 [English](README.md) | **Русский**

---

<img src="assets/logo.svg" width="48" height="48" align="left" style="margin-right: 14px">

# Secure Password Generator v1.0.2-devel

[![JavaScript](https://img.shields.io/badge/JavaScript-ES2020-f7df1e?logo=javascript&logoColor=black)](https://developer.mozilla.org/en-US/docs/Web/JavaScript)
[![License](https://img.shields.io/badge/license-MIT-green)](LICENSE.md)

**Лёгкий полностью клиентский генератор паролей и ключей. Поддерживает режимы Пароль, Фраза-пароль, PIN, SSH-ключ и GPG-ключ. Вся генерация происходит в браузере через Web Crypto API — никакие данные не передаются на сервер.**

---

## Демо

Текущая версия доступна по адресу **https://yukh.net/passgen/**

---

## Возможности

### Пароль
- Настраиваемая длина (4–64 символа, по умолчанию 20)
- Наборы символов: заглавные, строчные, цифры, специальные символы
- Исключение визуально похожих символов (I, l, 1, O, 0)
- Запрет повторяющихся символов
- Поля для дополнительных и исключаемых символов
- **8 пресетов для платформ**: PostgreSQL, MySQL, SQL Server, Linux User, Windows Admin, Redis, RabbitMQ, High Entropy
- Индикатор надёжности на основе энтропии Шеннона и разнообразия символов — **Очень сильный** требует ≥ 80 бит и 3+ типа символов

### Фраза-пароль
- Случайные наборы слов из встроенного словаря ~500 слов
- Настраиваемое количество слов (2–10, по умолчанию 8)
- Варианты регистра: с заглавной буквы или случайный для каждой буквы
- Опциональная цифра в начале и/или конце (приклеивается к слову без разделителя)
- Числа внутри фразы (случайно добавляет цифру к части слов)
- Разделитель: пробел, дефис (по умолчанию), точка, подчёркивание, случайный символ или без разделителя

### PIN
- Числовой код, настраиваемая длина (4–12 цифр, по умолчанию 6)

### SSH-ключ
- Генерация пар ключей **ED25519** или **RSA** (2048 / 3072 / 4096 бит) прямо в браузере
- Ключи в стандартном формате OpenSSH — совместимы с выводом `ssh-keygen`
- Опциональный **пароль ключа** шифрует приватный ключ (AES-256-CTR + bcrypt KDF, 16 раундов) — идентично выводу `ssh-keygen -a 16`
- Кнопка копирования для каждого ключа; **Скачать архив** сохраняет оба ключа одним ZIP-файлом (`<имя>.key` + `<имя>.pub`)
- Модальное окно **Проверить ключи**: вставить или загрузить приватный и публичный ключ и убедиться, что они образуют верную пару; ключи с паролем полностью поддерживаются — поддерживается загрузка файлов (`.key`, `.pub`, `.pem`)
- bcrypt и ZIP реализованы на чистом JS без внешних библиотек

### GPG-ключ
- Генерация пар ключей **Ed25519** или **RSA** (3072 / 4096 бит) прямо в браузере
- Ключи экспортируются в формате ASCII armor OpenPGP v4 (`-----BEGIN PGP PRIVATE KEY BLOCK-----`)
- Включает самоподписанный идентификатор пользователя — ключи импортируются через `gpg --import` без дополнительных шагов
- Опциональное поле **Идентификатор пользователя** (формат `Имя <email>`); при пустом поле используется стандартный UID
- Кнопка копирования для каждого ключа; **Скачать архив** сохраняет файлы `.asc` и `.pub.asc` одним ZIP
- Формат пакетов OpenPGP, CRC-24 и самоподпись реализованы на чистом JS без внешних библиотек

### Общее
- Генерация от 1 до 50 результатов за раз на всех вкладках
- Кнопка копирования у каждого результата; **Скопировать все** открывает модальное окно с textarea
- Кнопка «Пересоздать» — общая для всех результатов на вкладке
- Переключатель тёмной/светлой темы, переключатель языка EN / RU
- Активная вкладка и все настройки генератора сохраняются в `localStorage`; кнопка **сброса** (↺) возвращает все настройки по умолчанию
- Криптографически стойкая случайность через `window.crypto.getRandomValues()` с rejection sampling

---

## Установка

```bash
git clone https://github.com/yukh975/passgen.git
cd passgen
```

Откройте `index.html` в браузере — сборка не требуется, зависимостей нет.

Или запустите любой статический сервер:

```bash
npx serve .
```

---

## Структура проекта

```
passgen/
├── index.html              # Оболочка интерфейса
├── assets/
│   ├── app.js              # Логика генератора, настройки, i18n
│   ├── bcrypt.js           # Реализация bcrypt_pbkdf (чистый JS)
│   ├── pgp.js              # Генерация ключей OpenPGP v4 (чистый JS)
│   ├── translations.js     # Строки интерфейса (EN / RU)
│   ├── wordlist.js         # Встроенный словарь ~500 слов
│   ├── style.css           # Стили (тёмная / светлая тема)
│   ├── logo.svg            # Логотип с замком
│   └── theme-init.js       # Предзагрузка темы (без мерцания)
├── favicon.ico             # Сгенерирован из логотипа (16 / 32 / 48 px)
└── gen-favicon.js          # Генератор favicon (Node.js, без зависимостей)
```

---

## Безопасность

- Вся случайность берётся из `window.crypto.getRandomValues()` с rejection sampling для устранения смещения по модулю.
- Данные не покидают браузер — генерация полностью локальная.
- Нет внешних CDN, удалённых ресурсов и трекинга.

---

## Автор

Юрий Хачатурян (при поддержке [Claude.AI](https://claude.ai)), 2026.

---

🌐 [English](README.md) | **Русский**
